Select, native dropdown with a hairline chevron; matches TextField metrics.

```jsx
<Select id="topic" label="Topic" placeholder="Choose one" options={["Readings & events", "Rights & permissions", "Press", "Other"]} defaultValue="" />
```
