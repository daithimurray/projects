EmptyState, centred, quiet, one action.

```jsx
<EmptyState glyph="¶" title="Nothing here yet" description="No poems match “memory” and “father”. Try one at a time." actions={<Button variant="secondary">Clear filters</Button>} />
```
