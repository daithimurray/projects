NewsletterSignup, the site's one conversion moment; say what arrives and how often.

```jsx
<NewsletterSignup onSubmit={subscribe} success={done} />
<NewsletterSignup variant="inverse" title="A letter from Celbridge" />
```
