# Where the quote form POSTs (JSON). Leave empty until the client picks a form service.
PUBLIC_FORM_ENDPOINT=
# noindex is ON unless this is exactly 0. Set PUBLIC_NOINDEX=0 for the client's own domain.
PUBLIC_NOINDEX=1
# 1 = keep the design contract comment in the built HTML (review builds only).
PUBLIC_DESIGN_CONTRACT=0
